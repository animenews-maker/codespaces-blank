import logging

def post_to_instagram(image_path: str, caption: str = ""):
    # Placeholder for Instagram poster-only integration
    logging.info(f"Would post to Instagram: {caption} with image {image_path}")
    return True
